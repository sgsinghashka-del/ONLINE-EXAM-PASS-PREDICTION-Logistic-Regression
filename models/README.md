# Model artifacts

This directory is intentionally kept in the repository so the app can save trained models at runtime.

The actual model file is generated by `app.py` when the project is run locally, and it is ignored by the repository via `.gitignore`.
